# Core dependencies (Mac + Windows). Backtest works with these only.
pandas
pytest
yfinance
python-dotenv
openai
pyttsx3
requests

# MetaTrader5 is Windows-only. For paper/live: use Windows or Windows VPS and pip install -r requirements-windows.txt
